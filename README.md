# NeRF Reconstruction (Nerfstudio)

This repo contains my experiment using Nerfstudio to train and render a NeRF model.

## Setup

```bash
conda create -n nerfstudio python=3.10
conda activate nerfstudio
pip install nerfstudio

